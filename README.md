# task_planner
Help me create an app with vite react.

npm create vite@latest task_planner -- --template react
cd task_planner
# all npm actions should be inside the app dir
# npm install, respecets the deps defined in package.json
npm install
npm run dev 
npm install react-router-dom
npm install react-calendar